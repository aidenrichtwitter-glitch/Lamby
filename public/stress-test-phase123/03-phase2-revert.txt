===============================================================
PHASE 2: SIMULATING GROK REVERTING ALL CHANGES
===============================================================

OBJECTIVE: Simulate Grok undoing all Phase 1 changes via browse_page
and restoring the project to its pre-test state. This tests the safety
boundaries Grok will encounter when trying to clean up or undo work.

--- Step 1: Grok attempts git reset --hard ---
What Grok does: browse_page on grok-git with reset action
URL: /api/grok-git?project=groks-app&action=reset&args=--hard%20HEAD~1
Result: SANITIZED — bridge converted to: git reset --soft HEAD
Discovery: Bridge connector sanitizes --hard to --soft for safety.
Impact: Commit was undone but files remained on disk.
Lesson for Grok: Cannot do destructive resets. Must manually restore files.

--- Step 2: Grok deletes files via delete_file ---
What Grok does: browse_page on grok-proxy with delete_file actions
Deleted files:
  - src/hooks/useTheme.ts → PASS
  - src/pages/Analytics.tsx → PASS
  - src/_proxy_project_test.txt → PASS
  - src/_test_step1.txt → PASS

--- Step 3: Grok restores original stub files ---
What Grok does: browse_page on grok-proxy with write_file actions
Restored:
  - src/pages/Dashboard.tsx → stub (226 chars) → PASS
  - src/pages/Users.tsx → stub (214 chars) → PASS

--- Step 4: Grok commits the revert ---
What Grok does: browse_page on grok-git
Result: PASS — [master 3bac42d] Phase 2: revert Phase 1 stress test changes

=== CRITICAL INCIDENT: APP CRASH ===
After Grok deleted Analytics.tsx, the desktop app CRASHED with Vite error overlay:
  [plugin:vite:import-analysis] Failed to resolve import "../pages/Analytics"
  from "src/components/App.tsx". Does the file exist?

ROOT CAUSE:
  When Grok deleted Analytics.tsx, App.tsx still had:
    import Analytics from '../pages/Analytics'
  This broke the entire application — Vite error overlay, no content rendered.
  Grok would see this crash if it took a screenshot after the delete.

WHAT GROK SHOULD HAVE DONE:
  1. BEFORE deleting: browse_page grok-read to check App.tsx imports
  2. If App.tsx imports the file: write a replacement FIRST, then delete the old one
     OR update App.tsx to remove the import first
  3. AFTER any edit: browse_page grok-proxy + get_console_errors to verify no crashes

FIX APPLIED (simulating Grok's recovery):
  1. Read src/components/App.tsx via grok-read to confirm it imports Analytics
  2. Wrote new Analytics.tsx with proper default export via grok-proxy + write_file
  3. Verified via get_console_errors: totalErrors=0
  4. Screenshot confirmed: app rendering correctly again
  5. Git committed the fix via grok-git

LESSON FOR GROK (now in prompt template):
  ALWAYS check what imports a file before deleting it.
  A single broken import crashes the entire Vite application.
  Use get_console_errors after EVERY edit to catch this immediately.

PHASE 2 SUMMARY:
  - git reset --hard NOT possible via bridge (sanitized to --soft)
  - delete_file works but Grok MUST check imports first
  - Manual revert via delete_file + write_file is the only safe way
  - App crash incident discovered — rule added to Grok's prompt template
  - Final state: app running, 0 console errors, screenshot confirmed
