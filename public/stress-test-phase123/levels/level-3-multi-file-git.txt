=== LEVEL 3: MULTI-FILE + GIT COMMIT TEST ===
=== OBJECTIVE: Create a new page, wire it into routing and navigation, commit to git ===

This is the full stress test. You will:
- Create a brand new page component (Metrics.tsx)
- Add it to the app's routing (App.tsx PAGES map)
- Add it to the navigation menu (Navigation.tsx NAV_ITEMS)
- Verify everything compiles
- Commit to git

CRITICAL STOP RULES (READ THESE FIRST):
- You have a MAXIMUM of 30 browse_page calls for this entire test. If you exceed 30, STOP immediately.
- Once you post the FINAL coord note (L3 REVIEW), you are DONE. Do NOT make any more browse_page calls.
- Do NOT retry any step more than 2 times. If it fails twice, post the failure and move on.
- Do NOT loop back to earlier steps after completing later ones.
- Each grok-write call should be made EXACTLY ONCE per file. Do NOT call grok-write on the same file multiple times for the same change.

CRITICAL APPROACH — WHAT WORKS (TESTED AND VERIFIED):
- For CREATING new files (Metrics.tsx): Use grok-create-chunk (NOT grok-create).
  The file will be >1000 chars. browse_page has a ~2048 char URL limit. grok-create WILL FAIL.
  Split into ~800-char raw chunks. Send each as a separate GET:
  GET {{RELAY_BASE}}/api/grok-create-chunk?project={{PROJECT}}&path=FILE&content=CHUNK&chunk=N&total=TOTAL
  Chunks must be sent IN ORDER (0, 1, 2, ...). The file only appears on disk when the FINAL chunk lands.

- For EDITING App.tsx: Use grok-create (read → modify → write full file).
  DO NOT USE grok-write ON APP.TSX. The desktop runs on Windows with \r\n line endings.
  grok-write search strings use \n but the file has \r\n — this causes SILENT FAILURE (0 replacements).
  App.tsx is small (~920 chars), so grok-create with the full updated content works perfectly.

- For EDITING Navigation.tsx: grok-write WORKS for SINGLE-LINE replacements (no \r issue when
  matching within one line). Use it for the nav item. Do NOT call it more than ONCE.

- Activity icon is ALREADY imported in Navigation.tsx. Do NOT add a duplicate import.

STEPS (do them in order, post to coord after each):

0. CLEANUP — ENSURE CLEAN BASELINE (REQUIRED BEFORE EVERY L3 RUN):
   This step removes any leftover state from a previous L3 run so the test is idempotent.

   a) CHECK for stale Metrics.tsx:
      GET {{RELAY_BASE}}/api/grok-read?project={{PROJECT}}&path=src/pages/Metrics.tsx
      → If it exists (status: success), DELETE it:
        GET {{RELAY_BASE}}/api/grok-delete?project={{PROJECT}}&path=src/pages/Metrics.tsx

   b) CHECK App.tsx for stale Metrics references:
      GET {{RELAY_BASE}}/api/grok-read?project={{PROJECT}}&path=src/components/App.tsx
      → If the content contains "Metrics", you must remove it:
        - Remove the "import { Metrics }" line
        - Remove the "metrics: Metrics" entry from the PAGES map
        - Write the cleaned file back with grok-create (NOT grok-write — Windows \r\n issue)
        GET {{RELAY_BASE}}/api/grok-create?project={{PROJECT}}&path=src/components/App.tsx&content=CLEANED_CONTENT

   c) CHECK Navigation.tsx for stale metrics entries:
      GET {{RELAY_BASE}}/api/grok-read?project={{PROJECT}}&path=src/components/Navigation.tsx
      → If the content contains "'metrics'", you must remove it:
        - Remove any lines containing "id: 'metrics'" from the NAV_ITEMS array
        - Do NOT remove the Activity icon import (it's used by other items too)
        - Write the cleaned file back. Nav is large (~4200 chars), so use grok-create-chunk.

   d) CHECK Dashboard.tsx is clean:
      GET {{RELAY_BASE}}/api/grok-read?project={{PROJECT}}&path=src/pages/Dashboard.tsx
      → If it starts with "//" (stale L2 comment header), restore it:
        GET {{RELAY_BASE}}/api/grok-run?project={{PROJECT}}&command=git%20checkout%20HEAD%20--%20src/pages/Dashboard.tsx

   e) GIT CLEANUP: If any files were changed in cleanup steps a-d:
      GET {{RELAY_BASE}}/api/grok-git?project={{PROJECT}}&action=add&args=.
      GET {{RELAY_BASE}}/api/grok-git?project={{PROJECT}}&action=commit&message=L3-cleanup:%20remove%20stale%20artifacts
      (If "nothing to commit", that's fine — means it was already clean)

   f) VERIFY CLEAN STATE:
      GET {{RELAY_BASE}}/api/grok-git?project={{PROJECT}}&action=status
      → Should show "nothing to commit, working tree clean" (or only untracked files that aren't ours)

   g) CONSOLE CHECK after cleanup:
      GET {{RELAY_BASE}}/api/grok-proxy?project={{PROJECT}}&payload={"actions":[{"type":"get_console_errors","project":"{{PROJECT}}"}]}
      → Must be 0 errors. If errors exist, fix them before proceeding.

   → Post to coord: "L3-S0: cleanup done, baseline clean" or "L3-S0: cleanup done, had to remove [stale files]"
   → If cleanup fails, post: "L3-S0: cleanup FAILED: [details]" and STOP.

1. READ TREE: Call /api/grok-tree?project={{PROJECT}}
   → Post to coord: "L3-S1: tree read"

2. READ APP FILE: Call /api/grok-read?project={{PROJECT}}&path=src/components/App.tsx
   → Study it carefully. Note:
   - The exact import lines (e.g., "import Settings from '../pages/Settings'")
   - The PAGES map entries (e.g., "settings: Settings")
   - You will need the FULL CONTENT of this file for step 6
   - IMPORTANT: Save/remember this content exactly — you'll modify it and write it back
   → Post to coord: "L3-S2: App read"

3. READ NAVIGATION: Call /api/grok-read?project={{PROJECT}}&path=src/components/Navigation.tsx
   → Note the exact NAV_ITEMS array and which icons are ALREADY imported
   → Activity icon IS already imported — do NOT add it again
   → Post to coord: "L3-S3: Nav read, Activity already imported"

4. CREATE METRICS PAGE: Use grok-create-chunk (NOT grok-create).

   Build a Metrics.tsx component with these requirements:
   - Named export: export const Metrics: React.FC = () => { ... }
   - Default export: export default Metrics
   - At least 3 metric cards (bridge latency, uptime, requests)
   - useState and useEffect hooks
   - TypeScript types (interface MetricCard)
   - Dark mode support (dark: classes)
   - At least 80 lines of real code

   Split the content into 4-5 chunks of ~800 raw chars each. For each chunk:
   GET {{RELAY_BASE}}/api/grok-create-chunk?project={{PROJECT}}&path=src/pages/Metrics.tsx&content=CHUNK_TEXT&chunk=N&total=TOTAL
   - URL-encode each chunk's content
   - Keep each chunk's ENCODED content under ~1500 chars so the full URL stays under ~2000 chars
   - Send chunks IN ORDER: chunk=0, then chunk=1, then chunk=2, etc.
   - The file only appears on disk when the last chunk (chunk=total-1) arrives
   - DO NOT send chunks out of order or in parallel

   DO NOT use grok-create (single URL) for this — it WILL fail because the URL is too long.
   → Post to coord: "L3-S4: Metrics.tsx chunks sent"

5. VERIFY METRICS: Call grok-read for src/pages/Metrics.tsx
   → Confirm the first line is "import React"
   → Confirm it has "export const Metrics" and "export default Metrics"
   → Post to coord: "L3-S5: Metrics.tsx verified"
   → If MISSING or EMPTY: Retry step 4 ONCE. If still missing, post failure and stop.

6. UPDATE APP.TSX — USE GROK-CREATE (NOT grok-write):
   *** WINDOWS LINE ENDING WARNING: The desktop project uses \r\n line endings. ***
   *** grok-write search/replace WILL FAIL SILENTLY because it sends \n but the file has \r\n. ***
   *** This is a KNOWN BUG. The ONLY reliable approach for App.tsx is grok-create. ***

   Take the App.tsx content you read in step 2 and modify it:
   a) Add after the last import line: import { Metrics } from '../pages/Metrics'
   b) Add to the PAGES map after the last entry: metrics: Metrics (with comma after the previous entry)

   Then write the entire updated file:
   GET {{RELAY_BASE}}/api/grok-create?project={{PROJECT}}&path=src/components/App.tsx&content=FULL_UPDATED_CONTENT

   The file is ~920 chars so grok-create (single URL) works fine for this one.
   → Post to coord: "L3-S6: App.tsx updated via grok-create"

7. VERIFY APP: grok-read App.tsx
   → Confirm "import { Metrics }" is present
   → Confirm "metrics: Metrics" is present in the PAGES map
   → Confirm the file is >900 chars (if truncated to <100 chars, the write failed — restore and retry)
   → Post to coord: "L3-S7: App.tsx verified, import yes, route yes"
   → If EITHER missing: Read the file, fix it with grok-create, verify again. Do NOT continue without both.

8. UPDATE NAVIGATION: Use grok-write to add ONE metrics nav item.
   Search for the LAST nav item: { id: 'settings', label: 'Settings', icon: Settings }
   Replace with: { id: 'settings', label: 'Settings', icon: Settings },\n  { id: 'metrics', label: 'Metrics', icon: Activity }
   This is a SINGLE-LINE search so \r\n line endings do NOT affect it.
   DO NOT add an import for Activity — it is ALREADY imported.
   DO NOT call grok-write more than ONCE for this change.
   → Post to coord: "L3-S8: Navigation.tsx updated"

9. VERIFY NAVIGATION: grok-read Navigation.tsx
   → Count how many times "'metrics'" appears. Must be EXACTLY 1.
   → If more than 1: The edit was duplicated. Read the file, remove duplicates with grok-create, verify again.
   → Confirm no duplicate Activity imports
   → Post to coord: "L3-S9: Nav verified, metrics entries: 1"

10. CONSOLE CHECK: Use grok-proxy (raw JSON) + get_console_errors
    → Post to coord: "L3-S10: console clean" or "L3-S10: console errors: [details]"
    → If errors: Fix them before continuing. Do NOT commit with errors.

11. PRE-COMMIT SANITY CHECK (IMPORTANT):
    → grok-read Dashboard.tsx — confirm it does NOT start with "//" (no stale L2 comment headers)
    → If Dashboard.tsx is corrupted, restore it: grok-run command=git%20checkout%20HEAD%20--%20src/pages/Dashboard.tsx
    → grok-git status — confirm at least these files are modified/new: App.tsx, Navigation.tsx, Metrics.tsx
    → Post to coord: "L3-S11: pre-commit OK" or "L3-S11: pre-commit PROBLEM: [details]"

12. GIT ADD: /api/grok-git?project={{PROJECT}}&action=add&args=.
    → Post to coord: "L3-S12: git add done"

13. GIT COMMIT: /api/grok-git?project={{PROJECT}}&action=commit&message=L3:%20Add%20Metrics%20page%20with%20routing%20and%20navigation
    → CHECK the response. If "nothing to commit": something went wrong.
    → Post to coord: "L3-S13: committed [hash]" or "L3-S13: FAILED: [reason]"

14. VERIFY COMMIT: grok-git log&count=3 → confirm hash is present
    → Post to coord: "L3-S14: commit verified"

15. FINAL REVIEW: Post to coord:
    "L3 REVIEW: [PASS/FAIL] — Metrics [present/missing], App [updated/unchanged], Nav [updated/unchanged], console [clean/errors]"

    *** TEST IS NOW DONE. STOP. DO NOT MAKE ANY MORE BROWSE_PAGE CALLS. ***
    *** DO NOT RETRY ANY STEPS. DO NOT WRITE ANY MORE FILES. YOU ARE FINISHED. ***

=== PASS CRITERIA ===
- All coord notes posted (L3-S0 through L3-S14 + review)
- Metrics.tsx exists with real component code (80+ lines, named + default exports)
- App.tsx has Metrics import AND metrics entry in PAGES map
- Navigation.tsx has EXACTLY 1 metrics nav item, no duplicate imports
- Console is clean
- Dashboard.tsx is clean (no comment headers)
- Git commit exists with insertions (not just deletions)

=== FAIL CONDITIONS ===
- Any file missing or unchanged after write
- App.tsx truncated (<100 chars = truncation failure)
- Navigation.tsx has >1 metrics entry (= duplicate from parallel grok-write)
- Dashboard.tsx has comment headers (= stale L2 corruption)
- Git commit contains only deletions
- Console errors after edits
- Coord notes missing
- MORE THAN 30 browse_page calls used (indicates infinite loop — automatic FAIL)
