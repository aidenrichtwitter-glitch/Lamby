=== LEVEL 3: MULTI-FILE + GIT COMMIT TEST ===
=== OBJECTIVE: Create a new page, wire it into routing and navigation, commit to git ===

You will create Metrics.tsx, add it to App.tsx routing, add it to Navigation.tsx, and git commit.

*** SINGLE-EXECUTOR RULE — APPLIES TO ENTIRE TEST ***
*** ONLY the Leader executes. All agents/teammates THINK ONLY — no browse_page, no code execution. ***
*** If you have sub-agents: they draft content and suggest plans. YOU execute every action. ***
*** Agents calling browse_page in parallel has caused EVERY prior L3 failure (duplicates, corruption, race conditions). ***

STOP RULES:
- Maximum 50 browse_page calls. If you exceed 50, STOP and post your FAIL report.
- Once you post "L3 REVIEW", you are DONE. No more browse_page calls.
- You may retry failed steps up to 3 times each.

THIS TEST HAS 4 PHASES. Complete each phase fully before starting the next.

==============================
PHASE 1 — READ & CLEANUP (~6 calls)
==============================
Gather all information. No writes yet.

1a. CLEANUP — restore files to KNOWN CLEAN state (3 calls):
    *** DO NOT use "git checkout" without a commit hash — HEAD may contain previous L3 commits! ***
    *** Always restore from the known-clean commit: af7629a ***

    GET {{RELAY_BASE}}/api/grok-git?project={{PROJECT}}&action=checkout&args=af7629a%20--%20src/components/App.tsx
    GET {{RELAY_BASE}}/api/grok-git?project={{PROJECT}}&action=checkout&args=af7629a%20--%20src/components/Navigation.tsx
    GET {{RELAY_BASE}}/api/grok-git?project={{PROJECT}}&action=checkout&args=af7629a%20--%20src/pages/Dashboard.tsx

    Also delete stale Metrics if it exists (1 call):
    GET {{RELAY_BASE}}/api/grok-delete?project={{PROJECT}}&path=src/pages/Metrics.tsx
    → "File not found" = success. Do NOT retry.

1b. READ App.tsx (1 call):
    GET {{RELAY_BASE}}/api/grok-read?project={{PROJECT}}&path=src/components/App.tsx
    → Save the FULL CONTENT in memory — you will modify and rewrite it in Phase 3.

1c. READ Navigation.tsx (1 call):
    GET {{RELAY_BASE}}/api/grok-read?project={{PROJECT}}&path=src/components/Navigation.tsx
    → Note the NAV_ITEMS array. Note that Activity icon is ALREADY imported.

1d. OPTIONAL — Browse for inspiration (1-2 calls):
    You may browse the web for React metric/dashboard card examples to inspire your component.
    This is optional — skip if you already know what to write.

→ Post to coord: "L3-P1: reads done, App [N chars], Nav [N chars]"

==============================
PHASE 2 — PLAN (0 browse_page calls)
==============================
Do ALL planning in your head. Do NOT call browse_page during this phase.

Plan A — Metrics.tsx content:
  Generate a Metrics.tsx component that is BETWEEN 800 AND 900 CHARACTERS total.
  This size constraint is critical — keep it focused.

  Requirements:
  - Named export: export const Metrics: React.FC = () => { ... }
  - Default export: export default Metrics (MUST be the LAST line)
  - At least 3 metric cards (e.g. bridge latency, uptime, requests)
  - Use useState hook
  - Dark mode support (dark: classes)
  - Valid TypeScript/JSX

Plan B — Updated App.tsx content:
  Take the App.tsx you read in Phase 1 and modify it:
  a) Add import: import { Metrics } from '../pages/Metrics'
  b) Add to PAGES map: metrics: Metrics
  Prepare the FULL updated file content.

Plan C — Navigation.tsx edit:
  You will use a search/replace edit:
  Search: { id: 'settings', label: 'Settings', icon: Settings }
  Replace: { id: 'settings', label: 'Settings', icon: Settings },\n  { id: 'metrics', label: 'Metrics', icon: Activity }
  DO NOT add an Activity import — it is ALREADY imported.

==============================
PHASE 3 — EXECUTE (1 code execution — that's it!)
==============================
*** USE THE BRIDGE EXECUTOR — do NOT write your own Python script ***
*** Do NOT use browse_page for any writes — use the executor for EVERYTHING ***

Run ONE code execution with all your file operations as a plan.
The executor handles chunking, encoding, verification, git, and honest reporting automatically.

```python
RELAY = "{{RELAY_BASE}}"
PROJECT = "{{PROJECT}}"

plan = [
    # Step 1: Create Metrics.tsx (executor auto-chunks if >450 chars)
    {"action": "create", "path": "src/pages/Metrics.tsx", "content": """YOUR METRICS.TSX CONTENT FROM PLAN A"""},

    # Step 2: Rewrite App.tsx with Metrics import + route
    {"action": "create", "path": "src/components/App.tsx", "content": """YOUR UPDATED APP.TSX CONTENT FROM PLAN B"""},

    # Step 3: Edit Navigation.tsx (single-line search/replace)
    {"action": "edit", "path": "src/components/Navigation.tsx",
     "search": "{ id: 'settings', label: 'Settings', icon: Settings }",
     "replace": "{ id: 'settings', label: 'Settings', icon: Settings },\n  { id: 'metrics', label: 'Metrics', icon: Activity }"},

    # Step 4: Verify all files
    {"action": "verify", "path": "src/pages/Metrics.tsx", "contains": ["export const Metrics", "export default Metrics"]},
    {"action": "verify", "path": "src/components/App.tsx", "contains": ["import { Metrics }", "metrics:"]},
    {"action": "verify", "path": "src/components/Navigation.tsx", "contains": ["'metrics'"]},

    # Step 5: Check console for errors
    {"action": "console_check"},

    # Step 6: Git commit
    {"action": "git_add"},
    {"action": "git_commit", "message": "L3: Add Metrics page with routing and navigation"},
    {"action": "git_log", "count": 3},

    # Step 7: Post result
    {"action": "coord", "note": "L3-P3: executor complete, see summary"},
]

# Fetch and run the bridge executor
import urllib.request
exec(urllib.request.urlopen(f"{RELAY}/bridge_executor.py").read().decode())
```

*** IMPORTANT ***
- Replace the triple-quoted strings with your ACTUAL content from Plans A and B.
- Escape any backslashes or triple-quotes inside the content strings.
- The executor prints a full PASS/FAIL summary with exact char counts.
- This is ONE code execution. No agents can interfere. No race conditions.
- The executor auto-waits 15s between writes and verification steps.

→ After the executor finishes, read its output carefully.

==============================
PHASE 4 — REVIEW (1 browse_page call max)
==============================
Read the executor output. It already verified everything and printed a summary.

4a. If executor says PASS:
    Post to coord: "L3 REVIEW: PASS — [copy the executor's summary line]"
    → TEST IS DONE. STOP.

4b. If executor says FAIL:
    Read the specific failures from the output.
    You may retry by running the executor again with a fixed plan (up to 3 retries).
    If still failing after 3 retries:
    Post to coord: "L3 REVIEW: FAIL — [copy the executor's failure details]"
    → TEST IS DONE. STOP.

    *** DO NOT claim PASS if the executor said FAIL. Report what it reported. ***

=== PASS CRITERIA ===
- Metrics.tsx exists with valid exports and 800+ chars of real code
- App.tsx has Metrics import AND metrics entry in PAGES map
- Navigation.tsx has EXACTLY 1 metrics nav item, no duplicate imports
- Console is clean (0 errors)
- Git commit exists with insertions
- Executor summary says PASS

=== APPROACH REFERENCE ===
- ALL file operations go through the bridge executor (1 code execution)
- The executor handles: chunking, URL encoding, verification, console checks, git, honest reporting
- Activity icon: ALREADY imported in Navigation.tsx — do NOT add duplicate
- Executor is at: {{RELAY_BASE}}/bridge_executor.py
